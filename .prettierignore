## Uses .gitignore syntax and should mostly match that file
## so that we don't format what never ends up in SCM.

# artifacts
build
bundles
bundle-*.js
compiled
dist
lcov-report
public

# VS Code has its own special formatting
tsconfig.*
